#include <stdio.h>
/** 
 * main -A program that prints a line using puts 
 * return : 0 (success)
 */
int main(void)
{
        puts("\”Programming is like building a multilingual puzzle\n");
        return (0);
}
